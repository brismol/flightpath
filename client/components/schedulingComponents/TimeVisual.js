import React from 'react';

export const TimeVisual = () => {
  //divide this with a grid as well
  //so this spans all 4 cols, then is divided within, into 48 cols? double that so visual is to quater of an hour? dunno, or just straight up number of minutes per col
  return (
    <div id="timeVisualize">
      <hr></hr>
    </div>
  );
};

export default TimeVisual;
